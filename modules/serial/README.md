# Serial Module

Reserved responsibility:

- Reserve a module boundary for serial port discovery and connection flows.
- Keep platform-specific serial code outside core runtime and generic
  components.
- Provide terminal/log-oriented UI helpers for device tools.

No serial backend is selected yet.
