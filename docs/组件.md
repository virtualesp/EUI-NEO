# 组件

组件层位于 `components/`。它不是另一套渲染系统，也不直接持有后端 primitive；组件只负责把常用 DSL 结构封装成更短、更稳定的 builder 调用。

页面仍然由 `eui::Ui` 里的 `row`、`column`、`stack`、`rect`、`text`、`image`、`polygon` 等图元组成。组件内部也只声明 DSL 树，事件、hover、pressed、transition、dirty rect 和 framebuffer cache 都继续交给 内部 Runtime。

## 引入

通常只需要包含聚合头：

```cpp
#include "eui_neo.h"
```

这个头当前导出：

| 组件 | 用途 |
| --- | --- |
| `button` | 按钮，支持文字、图标、主题、禁用、右键回调 |
| `checkbox` | 受控复选框 |
| `radio` | 受控单选项 |
| `toggleSwitch` | 受控开关 |
| `progress` | 进度条，`value` 范围是 `0.0f - 1.0f` |
| `slider` | 滑块，`value` 范围是 `0.0f - 1.0f` |
| `input` | 文本输入框，支持单行/多行、选择、剪贴板快捷键 |
| `segmented` | 分段选择 |
| `stepper` | 受控数字步进器，支持不同进制和位数 |
| `tabs` | 标签页切换 |
| `scroll` | Runtime 滚动条 |
| `scrollView` | 自动测量内容高度的滚动容器 |
| `virtualList` | 固定行高虚拟列表 |
| `markdown` | MD4C Markdown 显示组件 |
| `dropdown` | 下拉选择 |
| `datePicker` | dialog 式日期选择器，受控 `open/date` |
| `timePicker` | dialog 式时间选择器，受控 `open/time` |
| `colorPicker` | dialog 式颜色选择器，受控 `open/value` |
| `dataTable` | 简单数据表格 |
| `dialog` | 模态对话框 |
| `sidebar` | 右侧抽屉式侧边栏容器 |
| `toast` | 右上角 toast，支持自动消失 |
| `tooltip` | 轻量提示浮层，支持文本内容和圆润指针 |
| `contextMenu` | 右键菜单 |
| `carousel` | 视差重叠轮播，支持拖拽、滚轮和指示点切换 |
| `lineChart` | 折线图 |
| `barChart` | 柱状图 |
| `pieChart` | 饼图 |
| `mouseArea` | 透明输入热区，封装点击、hover、拖拽、滚轮和右键 |
| `panel` | 套用主题的 `RectBuilder` |
| `card` | 带背景、边框、阴影和内容 inset 的卡片容器 |
| `layoutDebugOverlay` | 显式绘制 frame / padding / content 边界的布局调试覆盖层 |
| `text` | 套用主题文本色的 `TextBuilder` |
| `image` | 套用主题图片样式的 `ImageBuilder` |
| `theme` | 颜色和视觉 token 辅助函数 |
| `workshop::neumorphicButton` | 工坊拟态按钮，高定视觉组件示例 |
| `workshop::heartSwitch` | 工坊爱心开关，内联 SVG 图标与庆祝动画 |
| `workshop::tiltCard` | 工坊鼠标跟随倾斜卡片 |

## API 标准

公开文档只使用标准名称。新组件、新示例和后续重构都按本节口径收敛。

状态归属、受控组件、`ui.state<T>`、Runtime state binding 和 `Signal<T>` 双向绑定见 [状态模型](状态.md)。

标准组件入口：

| 类型 | 标准入口 |
| --- | --- |
| 表格 | `dataTable` |
| 图表 | `lineChart`、`barChart`、`pieChart` |
| Picker | `datePicker`、`timePicker`、`colorPicker` |
| 复合控件 | `toggleSwitch`、`scrollView`、`virtualList`、`mouseArea`、`contextMenu` |
| 文本包装 | `text` |

命名规则：

- 组件工厂统一使用 lowerCamelCase。
- 多词入口按单词边界驼峰，例如 `dataTable`、`toggleSwitch`、`scrollView`、`datePicker`、`lineChart`。
- Builder 方法使用 lowerCamelCase，状态名使用业务名词，事件名使用 `onXxx`。
- 文档、README、gallery 和新测试只写标准名称。

组件公共属性标准：

| 类别 | 标准属性 | 适用范围 |
| --- | --- | --- |
| 身份 | 构造参数 `id` | 所有组件都必须传入稳定 id，内部子节点使用 `id + ".name"` |
| 尺寸 | `size(width, height)` | 有明确外框的控件、容器、图表、弹层 |
| 内容 | `text(...)`、`title(...)`、`message(...)`、`content(...)` | 文本、弹层、容器和组合组件按语义选择一种 |
| 受控值 | `value(...)`、`checked(...)`、`selected(...)`、`index(...)`、`open(...)`、`visible(...)` | 业务状态由页面持有，组件只回调 next value |
| 变更事件 | `onChange(...)`、`onOpenChange(...)`、`onDismiss(...)`、`onAutoDismiss(...)` | 值变化统一用 `onChange`，`open` 统一用 `onOpenChange`，`visible` 反馈可用 dismiss 事件 |
| 可用状态 | `disabled(bool)` | 可交互组件应支持禁用态，禁用后不触发业务回调 |
| 视觉 | `theme(tokens)`、`style(style)`、`transition(...)` | `theme` 按主题重建样式，`style` 完整覆盖样式，动画走 `transition` |
| 层级 | `zIndex(value)` | 弹层、菜单、toast、tooltip 等 overlay 组件 |
| 坐标 | `position(x, y)`、`anchor(...)`、`screen(width, height)` | 浮层、菜单、tooltip、dialog/picker 等需要定位的组件 |

`Signal<T>` 绑定是受控组件的简写：普通值使用 `.bind(signal)`，弹层开关使用 `.bindOpen(signal)`，toast 可见性使用 `.bindVisible(signal)`。需要额外副作用时继续写显式 `value/open + onChange/onOpenChange`。

当前代码未完全对齐公共属性时，以本节作为后续 API 收敛目标；具体组件小节只列当前标准用法。

组件文档覆盖标准：

| 组件 | 用途 | 核心参数 | 状态归属 | 示例 |
| --- | --- | --- | --- | --- |
| `button` | 触发命令 | `text`、`icon`、`theme`、`disabled`、`onClick` | 页面处理回调副作用 | 本文、gallery |
| `checkbox` | 布尔选择 | `checked`、`bind`、`text`、`onChange` | 页面持有 `checked` | 本文、gallery |
| `radio` | 单选项 | `selected`、`bind`、`text`、`onChange` | 页面持有选中项 | 本文、gallery |
| `toggleSwitch` | 开关 | `checked`、`bind`、`text`、`onChange` | 页面持有 `checked` | 本文、gallery |
| `progress` | 只读进度 | `value`、`theme`、`transition` | 页面持有 `value` | 本文、gallery |
| `slider` | 连续数值选择 | `value`、`bind`、`onChange` | 页面持有 `value` | 本文、gallery |
| `input` | 文本输入 | `value`、`bind`、`placeholder`、`multiline`、`onChange` | 页面持有文本，组件交互态走 `ui.state` | 本文、gallery |
| `segmented` | 分段选择 | `items`、`selected`、`bind`、`onChange` | 页面持有 index | 本文、gallery |
| `stepper` | 数字步进 | `value`、`bind`、`step`、`min/max`、`base`、`onChange` | 页面持有数值 | 本文、gallery |
| `tabs` | 标签切换 | `items`、`selected`、`bind`、`onChange` | 页面持有 index | 本文、gallery |
| `scroll` | Runtime 滚动条 | `scrollStateId`、`offset`、`viewport`、`content` | Runtime 管理滚动状态，页面可同步 offset | 本文 |
| `scrollView` | 自动测量滚动容器 | `size`、`offset`、`content`、`contentKey`、`onChange` | Runtime 管理滚动状态，组件可缓存稳定内容高度 | 本文、gallery |
| `virtualList` | 固定行高虚拟列表 | `size`、`itemCount`、`rowHeight`、`offset`、`overscanViewports`、`row`、`onChange` | Runtime 管理滚动状态，页面持有 offset；组件只 compose 可见 slot | 本文、gallery |
| `markdown` | Markdown 显示 | `markdown`、`width`、`theme`、`style` | 无业务状态，解析和行内布局按 source / 样式缓存 | 本文、markdown_viewer |
| `dropdown` | 下拉选择 | `items`、`selected`、`bind`、`open`、`bindOpen`、`onChange`、`onOpenChange` | 页面持有 selected/open | 本文、gallery |
| `datePicker` | 日期选择 | `open`、`date`、`screen`、`onChange`、`onOpenChange` | 页面持有 open/date，组件 draft 走 `ui.state` | 本文、gallery |
| `timePicker` | 时间选择 | `open`、`time`、`minuteStep`、`screen`、`onChange`、`onOpenChange` | 页面持有 open/time，组件 draft 走 `ui.state` | 本文、gallery |
| `colorPicker` | 颜色选择 | `open`、`value`、`colors`、`screen`、`onChange`、`onOpenChange` | 页面持有 open/value，组件 draft 走 `ui.state` | 本文、gallery |
| `dataTable` | 表格展示 | `columns`、`rows`、`theme` | 页面持有数据 | 本文、gallery |
| `dialog` | 模态确认 | `open`、`screen`、`title`、`message`、`onOpenChange`、回调 | 页面持有 open | 本文、gallery |
| `sidebar` | 抽屉容器 | `open`、`size`、`drawerWidth`、`title`、`onOpenChange`、`content` | 页面持有 open，内部内容由页面声明 | 本文、gallery |
| `toast` | 临时反馈 | `visible`、`screen`、`title`、`message`、`duration` | 页面持有 visible | 本文、gallery |
| `tooltip` | 提示浮层 | `anchor`、`value`、`source`、`bounds` | 无业务状态，可绑定 hover 透明度 | 本文、图表内部使用 |
| `contextMenu` | 右键菜单 | `open`、`position`、`items`、`onSelect`、`onSelectPath`、`onOpenChange` | 页面持有 open/position，组件持有子菜单路径 | 本文、gallery |
| `carousel` | 轮播展示 | `items`、`index`、`onChange` | 页面持有 index，拖拽动画态走 `ui.state` | 本文、gallery |
| `lineChart` | 折线图 | `values`、`labels`、`title` | 页面持有数据 | 本文、gallery |
| `barChart` | 柱状图 | `values`、`labels`、`colors`、`title` | 页面持有数据 | 本文、gallery |
| `pieChart` | 饼图 | `values`、`labels`、`colors`、`title` | 页面持有数据，过渡动画态走 `ui.state` | 本文、gallery |
| `mouseArea` | 输入热区 | `size`、`onTap`、`onMove`、`onDrag`、`onScroll`、`onContextMenu` | 页面处理业务状态，组件手势态走 `ui.state` | 本文，carousel / tiltCard 内部使用 |
| `panel` | 主题矩形包装 | `size`、`theme`、`style`、底层 rect API | 无业务状态 | 本文、demo |
| `card` | 主题卡片容器 | `size`、`padding`、`theme`、`style`、`content` | 无业务状态，内部只声明背景和内容 Stack | 本文、layout_regression_viewer |
| `layoutDebugOverlay` | 布局边界调试覆盖层 | `width`、`height`、`inset`、`label` | 无业务状态，手动传入被观察区域尺寸 | 本文、layout_regression_viewer |
| `text` | 主题文本包装 | `text`、`fontSize`、`theme`、`style` | 无业务状态 | 本文、demo |
| `image` | 主题图片包装 | `source`、`cover`、`contain`、`theme`、`style` | 资源缓存归 Runtime | 本文 |
| `theme` | 主题 token 和视觉辅助 | `light/dark`、`pageVisuals`、`fieldVisuals` | 无业务状态 | 本文、gallery |
| `workshop::neumorphicButton` | 拟态按钮 | `size`、`text`、`theme`、`transition`、`onClick` | 视觉按压态走 `ui.state`，页面处理业务回调 | 本文、gallery More |
| `workshop::heartSwitch` | 爱心开关 | `size`、`checked`、`theme`、`transition`、`onChange` | 页面持有 checked，庆祝动画计时走 `ui.state` | 本文、gallery More |
| `workshop::tiltCard` | 鼠标跟随倾斜卡片 | `size`、`title`、`subtitle`、`theme`、`maxTilt`、`transition` | 组件持有纯视觉 pointer/tilt 状态 | 本文、gallery More |

## 基本约定

- 组件 id 必须稳定，内部子节点使用 `id + ".name"`。
- 组件 API 使用 lowerCamelCase，例如 `datePicker`、`timePicker`、`colorPicker`。
- 组件文档只写标准入口和标准方法名。
- 大多数组件是受控组件：页面传入当前值，组件通过回调返回 next value。
- 组件不保存业务状态，不提供生命周期钩子。
- 需要动画时优先使用组件的 `.transition(...)`，或者组件内部固定的 DSL transition。
- 弹层类组件一般提供 `.zIndex(...)`，页面需要保证它们排在普通内容之上。
- 弹层、侧边栏、菜单和浮层需要在遮罩或面板背景上放 interactive blocker，用透明或实体 hit rect 阻断下层 hover / click / focus。
- `.style(...)` 用于完全覆盖样式结构，`.theme(tokens)` 用于按当前主题重建样式。
- 组件内部不应该 `new` primitive、不绕过 Runtime、不自己读取原始鼠标状态。
- `components/workshop/` 用于高度定制、视觉风格强的创意组件；基础容器和通用控件仍放在 `components/` 根目录。

## Theme

主题定义在 `components/theme.h`，常用入口：

```cpp
const auto dark = components::theme::dark();
const auto light = components::theme::light();

const auto page = components::theme::pageVisuals(dark);
const auto field = components::theme::fieldVisuals(dark);
```

当前提供：

| API | 说明 |
| --- | --- |
| `color(r, g, b, a)` | 创建 `eui::Color` |
| `withAlpha(color, alpha)` | 替换 alpha |
| `withOpacity(color, opacity)` | 按比例调整 alpha |
| `light()` / `dark()` | 基础明暗主题 |
| `pageVisuals(tokens)` | 页面标题、正文、卡片、间距等 token |
| `fieldVisuals(tokens)` | 输入控件圆角、padding、focus、弹层阴影等 token |
| `resolveFieldFill(...)` | 按 hover / active 计算字段背景 |
| `buttonHover(...)` | 按主题计算按钮 hover 色 |
| `buttonPressed(...)` | 按主题计算按钮 pressed 色 |
| `border(...)` | 常用边框 |
| `buttonBorder(...)` | 按钮边框 |
| `shadow(...)` | 通用阴影 |
| `buttonShadow(...)` | 按钮阴影 |
| `panelShadow(...)` | 面板阴影 |
| `popupShadow(...)` | 弹层阴影 |

## 基础包装

`panel`、`text`、`image` 返回的仍然是 core DSL builder，所以可以继续链式调用底层能力。

```cpp
components::panel(ui, "card", tokens)
    .size(360.0f, 220.0f)
    .transition(0.18f)
    .build();

components::text(ui, "title", tokens)
    .size(420.0f, 48.0f)
    .text("EUI Gallery")
    .fontSize(38.0f)
    .horizontalAlign(eui::HorizontalAlign::Center)
    .verticalAlign(eui::VerticalAlign::Center)
    .build();

components::image(ui, "cover", tokens)
    .size(320.0f, 180.0f)
    .source("assets/icon.png")
    .cover()
    .build();
```

`text.h` 还提供 `titleTextStyle(tokens)`、`subtitleTextStyle(tokens)`、`bodyTextStyle(tokens)`，用于快速生成 `eui::TextStyle`。

### card / layoutDebugOverlay

`card` 是基础容器组件，用于统一背景、圆角、边框、阴影和内容 inset。它仍然只声明 `Stack + Rect + content Stack`，不会保存业务状态。内容回调内的坐标从 inset 后的内容区域开始计算。

```cpp
components::card(ui, "settings.card")
    .position(24.0f, 80.0f)
    .size(360.0f, 180.0f)
    .padding(18.0f)
    .theme(tokens)
    .content([&] {
        ui.text("settings.card.title")
            .size(324.0f, 28.0f)
            .text("Settings")
            .fontSize(20.0f)
            .verticalAlign(eui::VerticalAlign::Center)
            .build();
    })
    .build();
```

常用 API：`position`、`x`、`y`、`size`、`width`、`height`、`wrapContentHeight`、`padding`、`style`、`theme`、`color`、`radius`、`border`、`shadow`、`opacity`、`zIndex`、`content`。

`layoutDebugOverlay` 用于视觉定位布局问题。它只根据传入的 `width`、`height`、`inset` 绘制 frame / padding / content 边界，不读取目标组件的实际 frame。调试时需要把它放在和目标区域相同的位置和尺寸上；如果放进 Row / Column / Flow 作为纯覆盖层，应使用 `.ignoreLayout()`，避免调试层参与布局、撑开 wrapContent 或影响 scrollView 内容高度。

```cpp
ui.stack("settings.card.debug.anchor")
    .position(24.0f, 80.0f)
    .size(360.0f, 180.0f)
    .ignoreLayout()
    .content([&] {
        components::layoutDebugOverlay(
            ui,
            "settings.card.debug",
            360.0f,
            180.0f,
            18.0f,
            "frame / padding / content");
    })
    .build();
```

`tests/fixtures/layout_regression_viewer.cpp` 覆盖 `padding`、`wrapContentHeight`、`fill`、overlay 背景、flex 边界和 `scrollView` 内容高度；`tests/fixtures/text_alignment_viewer.cpp` 用于观察不同字体和文本类型的垂直居中基线。

## 控件

### button

```cpp
components::button(ui, "save")
    .size(180.0f, 54.0f)
    .theme(tokens)
    .icon(0xF0C7)
    .text("Save")
    .transition(0.18f)
    .onClick([] {
        // update page state
    })
    .build();
```

常用 API：`size`、`scale`、`text`、`icon`、`fontSize`、`iconSize`、`textColor`、`iconColor`、`style`、`theme`、`radius`、`opacity`、`disabled`、`translate`、`translateX`、`translateY`、`pressScale`、`border`、`shadow`、`colors`、`transition`、`onClick`、`onContextMenu`。

按钮内部是 `Stack + Rect + Row + Text`。按压缩放作用在外层 stack，背景、图标和文字会一起动。

### checkbox / radio / toggleSwitch

这些都是受控组件，页面传当前值，回调里写回状态。

```cpp
components::checkbox(ui, "remember")
    .theme(tokens)
    .checked(remember)
    .text("Remember me")
    .onChange([&](bool value) {
        remember = value;
    })
    .build();

components::radio(ui, "mode.a")
    .selected(mode == 0)
    .text("Mode A")
    .onChange([&](bool selected) {
        if (selected) {
            mode = 0;
        }
    })
    .build();

components::toggleSwitch(ui, "motion")
    .checked(enableMotion)
    .text("Motion")
    .onChange([&](bool value) {
        enableMotion = value;
    })
    .build();
```

`checkbox` 支持 `size`、`checked`、`text`、`fontSize`、`boxSize`、`style`、`theme`、`transition`、`onChange`。

`radio` 支持 `size`、`selected`、`text`、`fontSize`、`dotSize`、`style`、`theme`、`transition`、`onChange`。

`toggleSwitch` 支持 `size`、`checked`、`text`、`fontSize`、`trackSize`、`style`、`theme`、`transition`、`onChange`。

### progress / slider

```cpp
components::progress(ui, "loading")
    .theme(tokens)
    .size(320.0f, 14.0f)
    .value(progress)
    .build();

components::slider(ui, "volume")
    .theme(tokens)
    .size(320.0f, 34.0f)
    .value(volume)
    .onChange([&](float value) {
        volume = value;
    })
    .build();
```

`progress` 支持 `size`、`value`、`style`、`theme`、`transition`。

`slider` 支持 `size`、`value`、`style`、`theme`、`transition`、`onChange`。`value` 会 clamp 到 `0.0f - 1.0f`。滑块用透明 hit rect 处理点击和拖拽，拖拽时根据缓存 bounds 计算 next value。

### markdown

`markdown` 是基于 MD4C 的显示组件。它不进入 core primitive，也不执行 HTML/CSS；组件把 Markdown 解析结果组合成现有 DSL 图元。组件内部会按 source、宽度和文本样式缓存解析结果与行内排版结果，避免稳定 Markdown 在反复 compose 时重复解析和测量；缓存归属当前 `ui.state` 生命周期，不改变显示结果。

```cpp
components::markdown(ui, "doc")
    .theme(tokens)
    .width(720.0f)
    .markdown(markdownText)
    .wrapContentHeight()
    .build();
```

常用 API：`markdown`、`width`、`size`、`height`、`wrapContentHeight`、`margin`、`position`、`theme`、`style`、`zIndex`。

当前接入 MD4C 的 block/span/text 回调面：标题、段落、引用、无序/有序/任务列表、分隔线、fenced/indented code、HTML block、表格、表格对齐、emphasis、strong、link/autolink、image、inline code、strikethrough、LaTeX math、wiki link、underline、inline HTML、entity、soft/hard break 等。inline code、link/autolink、image、LaTeX math、wiki link、inline HTML、删除线和下划线会在组件内部 line-box 排版中映射为可见文本、胶囊或装饰线；HTML/CSS 不执行，图片不会加载真实资源，而是显示为占位文本。代码、引用、HTML block 和表格使用块级样式显示。

### mouseArea

`mouseArea` 是通用输入热区，不负责业务状态，也不画默认 UI。它适合给 canvas、图片、卡片、carousel、时间轴这类区域补一层统一的鼠标/触摸板输入 API。

```cpp
components::mouseArea(ui, "stage.input")
    .size(width, height)
    .scrollStep(0.18f)
    .maxScrollStep(1.0f)
    .dragThreshold(3.0f)
    .onTap([](const components::MouseEvent& event) {
        // event.x / event.y 是热区内逻辑坐标
    })
    .onHover([](bool hover) {
        // hover changed
    })
    .onMove([](const components::MouseEvent& event) {
        // hover 时持续返回热区内局部坐标，可用于 pointer tilt / spotlight
    })
    .onDragStart([](const components::MouseEvent& event) {
        // drag threshold crossed
    })
    .onDrag([](const components::MouseDragEvent& event) {
        // event.totalX / totalY 是从按下开始的逻辑位移
    })
    .onScroll([](const components::MouseScrollEvent& event) {
        // event.stepY 已按 scrollStep 和 maxScrollStep 归一
    })
    .onContextMenu([](const components::MouseEvent& event) {
        // right click
    })
    .build();
```

常用 API：`position`、`size`、`zIndex`、`radius`、`color`、`cursor`、`disabled`、`scrollStep`、`maxScrollStep`、`dragThreshold`、`suppressClickAfterDrag`、`onTap`、`onPress`、`onRelease`、`onHover`、`onEnter`、`onLeave`、`onMove`、`onDragStart`、`onDrag`、`onDragEnd`、`onScroll`、`onContextMenu`。

`MouseEvent` 提供局部坐标 `x/y`、全局逻辑坐标 `globalX/globalY`、本帧位移 `deltaX/deltaY`、热区 bounds 和按键状态。`MouseDragEvent` 额外提供 `totalX/totalY`。`MouseScrollEvent` 提供原始滚轮 `deltaX/deltaY` 和处理后的 `stepX/stepY`。

### input

```cpp
components::input(ui, "name")
    .theme(tokens)
    .size(280.0f, 42.0f)
    .value(name)
    .placeholder("Hello EUI-NEO 😉")
    .fontFamily("monospace")
    .onChange([&](const std::string& value) {
        name = value;
    })
    .onEnter([] {
        // submit
    })
    .build();
```

支持 `size`、`value`、`placeholder`、`multiline`、`fontSize`、`fontFamily`、`inset`、`style`、`theme`、`transition`、`onChange`、`onEnter`、`onFocus`。

当前 input 支持 focus、点击定位光标、拖动选择、Shift+方向键选择、Ctrl+A/C/X/V/Z、Cmd+A/C/X/V/Z、Ctrl+Y、Ctrl+Shift+Z、Cmd+Shift+Z、Backspace、Delete、Enter、单行和多行模式，并会把系统 IME 候选框锚定到当前光标位置。Windows/SDL 文本编辑事件会把 IME 预编辑文字透传给 input，并在光标处按输入框字体自绘组合串；Windows 下会抑制系统组合窗口，保留系统候选窗口。光标、选择、删除、撤销、重做和横向滚动基于 `TextPrimitive::measureTextMetrics(...)` 的 shaped caret stops；这些 stops 使用 UTF-8 byte offset，并与实际渲染的 fallback、emoji 缩放和 glyph advance 保持一致，适配 emoji、CJK、阿拉伯连字和组合符号。多行模式会按完整内容高度声明内部文本 frame，再由外层 input clip 裁剪，因此滚动到较多行时不会因为子元素 frame 离开 viewport 而被 Runtime 跳过绘制。默认字体族是跨平台 `monospace`；如果系统等宽字体路径不存在，会继续尝试默认 UI 字体和系统 UI 字体兜底。默认占位文本是 `Hello EUI-NEO 😉`。

`input` 只有在多行模式且文本内容确实可以纵向滚动时，才会注册内部滚轮处理。单行输入框、内容未超出输入框的多行输入框不会消费滚轮事件；鼠标停在这类输入框上时，外层 `scrollView` 仍然可以继续滚动。

### segmented / tabs

```cpp
components::segmented(ui, "density")
    .items({"Compact", "Normal", "Comfort"})
    .selected(density)
    .onChange([&](int index) {
        density = index;
    })
    .build();

components::tabs(ui, "main.tabs")
    .items({"Style", "Bing", "Settings"})
    .selected(tab)
    .onChange([&](int index) {
        tab = index;
    })
    .build();
```

两者都支持 `size`、`items`、`selected`、`fontSize`、`style`、`theme`、`transition`、`onChange`。

### stepper

`stepper` 是紧凑的受控数字步进器，适合调整数值、寄存器值、不同进制编号等场景。组件内部提供减号按钮、格式化数值显示和加号按钮。

```cpp
components::stepper(ui, "register.hex")
    .size(190.0f, 40.0f)
    .value(registerValue)
    .step(1)
    .base(16)
    .bitWidth(16)
    .theme(tokens)
    .onChange([&](long long value) {
        registerValue = static_cast<int>(value);
    })
    .build();
```

支持：

- `size`
- `value`
- `step`
- `min`
- `max`
- `base`
- `digits`
- `bitWidth`
- `showBasePrefix`
- `prefix`
- `uppercase`
- `fontSize`
- `style`
- `theme`
- `transition`
- `onChange`

说明：

- `base` 当前支持 `2 - 36`。
- `digits` 用于固定显示位数，不足时左侧补 `0`。
- `bitWidth` 会在未显式设置 `min/max` 时自动把范围限制为 `0` 到该位宽可表示的最大值，并推导常见显示位数，例如 `8-bit binary -> 8 digits`、`16-bit hex -> 4 digits`。
- `showBasePrefix(true)` 时，二进制/八进制/十六进制默认显示 `0b` / `0o` / `0x` 前缀；也可以通过 `prefix(...)` 自定义。

### scroll

`scroll` 只绘制滚动条，并把 track/thumb 绑定到 Runtime 滚动状态。它不直接持有内容；内容裁剪和位移应交给同一个 Runtime scroll state，通常由 `scrollView` 统一创建。

```cpp
ui.stack("list.viewport")
    .size(360.0f, 260.0f)
    .clip()
    .scrollState("list.viewport", scrollY, contentHeight - 260.0f, 48.0f)
    .onScrollOffsetChanged([&](float value) {
        scrollY = value;
    })
    .content([&] {
        ui.column("list.content")
            .size(360.0f, contentHeight)
            .scrollContentFrom("list.viewport")
            .build();
    })
    .build();

components::scroll(ui, "list.scroll")
    .scrollStateId("list.viewport")
    .size(8.0f, 260.0f)
    .viewport(260.0f)
    .content(contentHeight)
    .offset(scrollY)
    .build();
```

支持 `x`、`y`、`position`、`size`、`scrollStateId`、`offset`、`viewport`、`content`、`step`、`zIndex`、`style`、`theme`、`transition`。

### scrollView

`scrollView` 是在 `scroll` 之上的更高层封装：它负责创建 viewport、自动测量内容高度、在需要时显示滚动条，并把当前 offset 回传给页面状态。滚轮和拖动滚动条时，Runtime 只更新滚动 transform 和 viewport dirty rect，不重新 compose 整个页面。

```cpp
components::scrollView(ui, "page.body")
    .theme(tokens)
    .size(640.0f, 420.0f)
    .offset(scrollY)
    .gap(18.0f)
    .step(48.0f)
    .onChange([&](float value) {
        scrollY = value;
    })
    .content([&](eui::Ui& contentUi, float contentWidth, float viewportHeight) {
        contentUi.column("page.sections")
            .width(contentWidth)
            .height(eui::SizeValue::wrapContent())
            .gap(18.0f)
            .content([&] {
                // compose scroll content here
            })
            .build();
    })
    .build();
```

支持 `size`、`offset`、`gap`、`step`、`scrollbarWidth`、`scrollbarGap`、`zIndex`、`style`、`theme`、`transition`、`contentKey`、`onChange`、`content(...)`。

使用建议：

- 需要“滚动区域自动更新，不想手动维护内容高度”时，优先用 `scrollView`。
- `scrollView` 会先按内容宽度测量；若出现滚动条，再按扣除滚动条后的宽度重测一次，避免文本换行和内容高度不一致。
- 内容高度稳定、但 compose 成本较高时，可以传 `contentKey(...)` 复用测量高度；key 必须包含会改变内容高度的状态，例如主题密度、展开项、数据版本或筛选条件。未传 key 时保持每次自动测量。
- 如果内容测量要完全自定义，可以使用底层 `scroll`，但滚动内容和滚动条必须绑定同一个 Runtime scroll state。

### virtualList

`virtualList` 是固定行高的纵向虚拟列表。它用 `itemCount * rowHeight` 作为总滚动高度，但只 compose 当前 viewport 和 overscan 范围内的可见 slot，适合几千到几百万行的列表。行回调里的 `index` 是真实数据行号，`rowId` 是可复用的可见 slot id；业务状态应该按真实 index 或数据 key 存在页面层，不要绑在 slot id 上。

```cpp
float listOffset = 0.0f;

components::virtualList(ui, "logs.virtual")
    .theme(tokens)
    .size(520.0f, 300.0f)
    .itemCount(1000000)
    .rowHeight(32.0f)
    .offset(listOffset)
    .step(64.0f)
    .overscanViewports(1.0f)
    .onChange([&](float value) {
        listOffset = value;
    })
    .row([&](eui::Ui& rowUi, const std::string& rowId, auto index, float rowWidth, float rowHeight) {
        rowUi.text(rowId + ".text")
            .size(rowWidth, rowHeight)
            .text("row " + std::to_string(index))
            .fontSize(14.0f)
            .lineHeight(18.0f)
            .verticalAlign(eui::VerticalAlign::Center)
            .build();
    })
    .build();
```

支持 `x`、`y`、`position`、`size`、`itemCount`、`rowHeight`、`offset`、`bind`、`step`、`overscanViewports`、`scrollbarWidth`、`scrollbarGap`、`zIndex`、`style`、`theme`、`transition`、`onChange`、`row(...)`。

使用建议：

- 固定行高、数据量很大时用 `virtualList`；普通复杂页面或内容高度由子元素自然撑开时继续用 `scrollView`。
- 行内容应根据传入的真实 `index` 渲染，slot id 只用于内部元素稳定复用。
- 组件内部使用局部坐标渲染可见行，避免超大滚动 offset 下的 float 精度问题。

## 弹层和反馈

### dialog

```cpp
components::dialog(ui, "confirm")
    .open(showDialog)
    .screen(screen.width, screen.height)
    .title("Confirm")
    .message("Apply these changes?")
    .primaryText("Apply")
    .secondaryText("Cancel")
    .onPrimary([&] {
        showDialog = false;
    })
    .onSecondary([&] {
        showDialog = false;
    })
    .onOpenChange([&](bool open) {
        showDialog = open;
    })
    .build();
```

支持 `open`、`screen`、`size`、`title`、`message`、`primaryText`、`secondaryText`、`style`、`theme`、`transition`、`zIndex`、`onPrimary`、`onSecondary`、`onOpenChange`。

`dialog` 会绘制 backdrop，并用透明 hit rect 吃掉后面内容的 hover、click 和 scroll。打开/关闭动画作用在整体面板上，内容跟着外层一起动。

### sidebar

```cpp
components::sidebar(ui, "workshop.sidebar")
    .theme(themeColors())
    .size(screen.width, screen.height)
    .drawerWidth(430.0f)
    .open(showSidebar)
    .title("Workshop")
    .onOpenChange([&](bool open) {
        showSidebar = open;
    })
    .content([&](eui::Ui& panelUi, float width, float height) {
        panelUi.text("workshop.empty")
            .size(width, 28.0f)
            .text("Place custom workshop components here.")
            .fontSize(16.0f)
            .lineHeight(22.0f)
            .build();
    })
    .build();
```

支持 `size`、`drawerWidth`、`open`、`title`、`eyebrow`、`theme`、`transition`、`zIndex`、`onOpenChange`、`content(...)`。

`sidebar` 是基础抽屉容器，不属于工坊组件本身。默认只渲染标题、eyebrow 和关闭按钮，面板内部内容通过 `content(...)` 声明。打开时左侧 scrim 负责点击关闭并阻断背景 click/scroll/focus，右侧面板背景会吃掉空白区域的 click/scroll/focus，避免事件穿透到底层页面。Runtime 的 hit-test 会从最上层往下找；命中 sidebar 的 interactive scrim 或 panel 背景后会停止继续查找，因此底层输入框不会获得焦点。抽屉动效默认使用 `OutExpo` 和约 `0.92s` 的时长，个人觉得这种“先快速出现，再慢慢收住”的交互手感最好。

### workshop

`components/workshop/` 放高度定制化组件，适合从网页创意组件、动效片段或品牌化控件迁移到 EUI-NEO。它们仍然通过 `components/components.h` 和 `eui_neo.h` 导出，但命名空间放在 `components::workshop` 下，避免和基础组件混在一起。

```cpp
components::workshop::neumorphicButton(ui, "demo.neumorphic")
    .theme(themeColors())
    .size(310.0f, 92.0f)
    .fontSize(32.0f)
    .text("Click me")
    .transition(pageTransition())
    .onClick([] {
        // update page state
    })
    .build();
```

`neumorphicButton` 使用 `Stack + Rect + Text` 组合拟态按钮：凸起态由两层外阴影构成，按压态使用底层 `.insetShadow(...)` 绘制裁剪在圆角内部的内阴影。组件会按 `.theme(tokens)` 派生明暗模式颜色，并尽量和所在容器 surface 衔接。

```cpp
components::workshop::heartSwitch(ui, "demo.heart")
    .theme(themeColors())
    .size(64.0f, 64.0f)
    .checked(liked)
    .onChange([&](bool value) {
        liked = value;
    })
    .build();
```

`heartSwitch` 复刻网页 SVG 爱心开关：outline / filled 图标 path 内联在组件头文件中，并通过一等 DSL SVG 源 `ui.svg(...).source(svg)` 渲染。这样组件不依赖额外 asset 文件，不写临时 SVG 文件，也避免用基础 rect / polygon 近似 SVG 形状。

```cpp
components::workshop::tiltCard(ui, "demo.tilt")
    .theme(themeColors())
    .size(318.0f, 178.0f)
    .title("Tilt Card")
    .subtitle("Pointer driven perspective card.")
    .maxTilt(0.22f)
    .build();
```

`tiltCard` 使用 `Stack + Rect + Text + mouseArea` 组合卡片。组件内部用 `mouseArea` 提供稳定命中区，再通过 Runtime 级 `.runtimePointerTiltFrom(...)` 读取当前指针位置并直接驱动 `.rotateX(...)` / `.rotateY(...)` / `.perspective(...)` 变换，避免 hover 移动时反复触发组件重组。更通用的高频 pointer-follow 视觉变换可用 `.runtimePointerTransformFrom(...)` 直接驱动 rotate / translate / hover scale；只有 Runtime 暂时无法表达，或确实需要回调更新业务状态时，才应在组件内使用 `mouseArea.onMove(...)`。

工坊组件允许持有少量纯视觉交互态，例如按下时切换内外阴影，但业务状态仍应留在页面或业务层。迁移网页组件时参考 [components/workshop/SKILL.md](../components/workshop/SKILL.md)。

### toast

```cpp
components::toast(ui, "saved.toast")
    .visible(showToast)
    .screen(screen.width, screen.height)
    .title("Saved")
    .message("Your changes were saved.")
    .icon(0xF00C)
    .duration(2.4f)
    .onAutoDismiss([&] {
        showToast = false;
    })
    .onDismiss([&] {
        showToast = false;
    })
    .build();
```

支持 `visible`、`screen`、`size`、`title`、`message`、`icon`、`style`、`theme`、`transition`、`zIndex`、`duration`、`onAutoDismiss`、`onDismiss`。

`duration(0.0f)` 表示不自动消失。关闭按钮是内部命中区，点击触发 `onDismiss`。

### tooltip

```cpp
components::tooltip(ui, "usage.tip")
    .anchor(120.0f, 88.0f)
    .value("32%")
    .source("usage.point.0")
    .bounds(206.0f, 236.0f)
    .build();
```

支持 `anchor`、`value`、`source`、`bounds`、`style`、`theme`、`zIndex`。`source(...)` 会把 tooltip 透明度绑定到目标元素 hover。

tooltip 使用 `Rect + Text + Polygon` 组合绘制，指针基于支持圆角和独立 shader 抗锯齿的 `Polygon`，避免尖角锯齿感。图表组件复用同一个 tooltip 实现。

### contextMenu

```cpp
components::contextMenu(ui, "item.menu")
    .open(menuOpen)
    .screen(screen.width, screen.height)
    .position(menuX, menuY)
    .items(std::vector<components::ContextMenuItem>{
        {"Rename"},
        {"Copy", {{"Token"}, {"Style", {{"CSS"}, {"JSON"}}}}},
        {"Delete"}
    })
    .onSelectPath([&](const std::vector<int>& path) {
        menuOpen = false;
        selectedActionPath = path;
    })
    .onOpenChange([&](bool open) {
        menuOpen = open;
    })
    .build();
```

`ContextMenuItem` 使用 `children` 递归描述任意层级。悬停含子项的菜单项会展开下一列；子菜单会根据屏幕空间自动向左或向右展开，并保持在屏幕纵向范围内。`onSelectPath` 返回从根到叶子的索引路径，例如 `{1, 1, 0}`。原有的 `items(std::vector<std::string>)` 和 `onSelect(int)` 保持兼容，适用于单级菜单。

支持 `open`、`screen`、`position`、`size(width, itemHeight)`、`items`、`style`、`theme`、`transition`、`zIndex`、`onSelect`、`onSelectPath`、`onOpenChange`。

右键菜单会根据 `screen` 把弹层限制在屏幕内，并绘制全屏透明 dismiss 层。菜单项 hover 使用即时状态，快速 hover 不会等待颜色动画拖尾。

### dropdown

```cpp
components::dropdown(ui, "theme.dropdown")
    .items({"Dark", "Light", "System"})
    .selected(themeIndex)
    .open(dropdownOpen)
    .placeholder("Theme")
    .onOpenChange([&](bool value) {
        dropdownOpen = value;
    })
    .onChange([&](int index) {
        themeIndex = index;
    })
    .build();
```

支持 `size`、`items`、`selected`、`placeholder`、`open`、`itemHeight`、`style`、`theme`、`transition`、`zIndex`、`onChange`、`onOpenChange`。

下拉面板会随外层 stack 展开，选项 hover 和右键菜单一致，关闭时选项会禁用命中，避免点到隐藏面板。

### datePicker / timePicker / colorPicker

这三个 picker 都是受控组件，命名使用 lowerCamelCase。页面传入当前值和 `open`，组件通过 `onChange` / `onOpenChange` 回写 next state。

picker 现在和 `dialog` 一样绘制全屏 overlay + 居中面板，适合放在页面根级 compose 末尾，避免被滚动容器或 `.clip()` 裁剪。触发入口可以用 `button` 或自定义字段单独绘制。

面板打开后会维护组件内部 draft：日期/时间轮盘、滚轮、颜色 slider、预设色块都只更新面板内预览，不会立即改变页面状态。点击 `Done` 才触发 `onChange` 提交 draft；点击遮罩只关闭，不提交。

日期和时间轮盘的选项会按离选中行的距离做透视式视觉衰减：选中项保持正面和满透明，邻近项逐步降低透明度、纵向压缩并向中心收拢。这个效果使用 Text transform 和 opacity 实现，不改变 picker 的选值逻辑。

```cpp
components::datePicker(ui, "profile.date")
    .theme(tokens)
    .screen(screen.width, screen.height)
    .size(420.0f, 270.0f)
    .date(year, month, day)
    .open(dateOpen)
    .onOpenChange([&](bool open) {
        dateOpen = open;
    })
    .onChange([&](int nextYear, int nextMonth, int nextDay) {
        year = nextYear;
        month = nextMonth;
        day = nextDay;
    })
    .build();

components::timePicker(ui, "profile.time")
    .theme(tokens)
    .screen(screen.width, screen.height)
    .size(330.0f, 264.0f)
    .time(hour, minute)
    .minuteStep(5)
    .open(timeOpen)
    .onOpenChange([&](bool open) {
        timeOpen = open;
    })
    .onChange([&](int nextHour, int nextMinute) {
        hour = nextHour;
        minute = nextMinute;
    })
    .build();

components::colorPicker(ui, "profile.color")
    .theme(tokens)
    .screen(screen.width, screen.height)
    .size(420.0f, 320.0f)
    .value(accent)
    .open(colorOpen)
    .onOpenChange([&](bool open) {
        colorOpen = open;
    })
    .onChange([&](eui::Color value) {
        accent = value;
    })
    .build();
```

`datePicker` 支持 `open`、`screen`、`size(panelWidth, panelHeight)`、`date(year, month, day)`、`style`、`theme`、`transition`、`zIndex`、`onChange`、`onOpenChange`。年份范围是 `1900 - 2200`，月份和日期会按闰年和当月天数修正，`onChange` 只在 `Done` 时触发。

`timePicker` 支持 `open`、`screen`、`size(panelWidth, panelHeight)`、`time(hour, minute)`、`minuteStep`、`style`、`theme`、`transition`、`zIndex`、`onChange`、`onOpenChange`。内部显示 12 小时轮盘，回调仍返回 `0 - 23` 的小时和 `0 - 59` 的分钟，`onChange` 只在 `Done` 时触发。

`colorPicker` 支持 `open`、`screen`、`size(panelWidth, panelHeight)`、`value(eui::Color)`、`colors(std::vector<eui::Color>)`、`style`、`theme`、`transition`、`zIndex`、`onChange`、`onOpenChange`。颜色 alpha 会按 `1.0f` 处理，面板里提供 RGB slider 和预设色块，`onChange` 只在 `Done` 时触发。

picker 的拖动、滚轮和 slider 交互只捕获 Loader scope 下的 draft / drag state，不捕获临时 builder 指针；外部 `onChange` 只在 `Done` 的命中区触发。

## 数据展示

### dataTable

```cpp
components::dataTable(ui, "metrics.table")
    .size(420.0f, 174.0f)
    .columns({"Name", "Status", "Value"})
    .rows({
        {"Cache", "Ready", "94%"},
        {"Render", "Idle", "12ms"},
        {"Network", "Online", "32ms"},
    })
    .theme(tokens)
    .build();
```

支持 `size`、`columns`、`rows`、`style`、`theme`、`transition`。

表格内部会裁剪内容区，表头和末行圆角用 patch rect 衔接，避免圆角和分割线断开。

### carousel

```cpp
components::carousel(ui, "hero.carousel")
    .size(720.0f, 260.0f)
    .items({
        {"assets/cover-a.jpg", "Cover A", "Subtitle A"},
        {"assets/cover-b.jpg", "Cover B", "Subtitle B"},
        {"assets/cover-c.jpg", "Cover C", "Subtitle C"},
    })
    .index(activeIndex)
    .cardWidthRatio(0.68f)
    .overlap(0.36f)
    .parallax(28.0f)
    .theme(tokens)
    .onChange([&](float next) {
        activeIndex = next;
    })
    .build();
```

支持 `size`、`items`、`index`、`cardWidthRatio`、`overlap`、`parallax`、`style`、`theme`、`transition`、`onChange`。`CarouselItem` 包含 `source`、`title`、`subtitle`。组件内部使用 `mouseArea` 处理拖拽、滚轮和卡片点击，`onChange` 返回浮点 index，方便拖拽时做连续位置更新。

## 图表

三个图表都是通用组件，使用 DSL 图元绘制，不使用 SVG 图片。tooltip 由独立 `components::tooltip` 组件绘制，并通过 `hoverOpacityFrom(sourceId)` 绑定到对应数据点、柱或扇区。

### lineChart

```cpp
components::lineChart(ui, "usage.line")
    .size(206.0f, 236.0f)
    .title("Usage")
    .values({0.18f, 0.32f, 0.48f, 0.42f, 0.72f, 0.64f})
    .labels({"Jan", "Feb", "Mar", "Apr", "May", "Jun"})
    .style(components::LineStyle::Curve)
    .theme(tokens)
    .build();
```

支持 `size`、`title`、`values`、`labels`、`style`、`theme`、`transition`。线型用 `.style(components::LineStyle::Linear / Curve / Step)` 设置；折线段会生成 capsule polygon，并走 polygon shader 边缘抗锯齿；点是单层交互圆，hover 时显示对应 label 和百分比。

### barChart

```cpp
components::barChart(ui, "usage.bar")
    .size(206.0f, 236.0f)
    .title("Volume")
    .values({0.92f, 0.36f, 0.68f, 0.52f})
    .labels({"D1", "D2", "D3", "D4"})
    .theme(tokens)
    .build();
```

支持 `size`、`title`、`values`、`labels`、`colors`、`style`、`theme`、`transition`。每根柱子独立 hover，tooltip 锚点位于柱顶。

### pieChart

```cpp
components::pieChart(ui, "usage.pie")
    .size(206.0f, 236.0f)
    .title("Share")
    .values({42.0f, 24.0f, 18.0f, 16.0f})
    .labels({"Blue", "Green", "Orange", "Pink"})
    .theme(tokens)
    .build();
```

支持 `size`、`title`、`values`、`labels`、`colors`、`style`、`theme`、`transition`。扇区使用 core 的 `polygon` 图元绘制，边缘由独立 polygon shader 抗锯齿；负数按 `0` 处理，显示值按总和归一化为百分比。

## 当前限制

- DSL 已提供 `ui.loader(...).destroyOnHide()` / `.keepAlive()` 和 `ui.state<T>(id)` 作为实例状态基础；组件私有交互态应接入 `ui.state`，由当前页面和 Loader scope 管理生命周期。
- `scroll` 只负责 offset 控制，不是完整 ScrollArea；需要自动测量内容时应优先用 `scrollView`。
- `input` 会在控件内自绘 IME 预编辑组合串，并继续使用系统 IME 候选窗口。
- chart 是轻量展示组件，当前只显示归一化百分比，不提供坐标轴刻度配置、图例布局和数据格式化回调。
- `dialog`、`toast`、`contextMenu`、`dropdown`、`datePicker`、`timePicker`、`colorPicker` 需要页面传入 open/visible 状态，组件不会自己持久化开关状态。
- 容器 transform 会继承到组件内部子树渲染，包含 2D transform 和 2.5D 透视投影；默认 hit-test 仍按布局矩形计算，组件确实需要跟随视觉变换时可在内部元素上开启 `.transformedHitTest()`。
- Runtime 按绘制顺序从最上层往下命中 interactive 元素，同一位置只会触发最上层交互层；focus 命中会被上层 interactive blocker 阻断。
- 复杂嵌套滚动、圆角 clip、事件冒泡等能力仍属于 Runtime 后续增强范围。

## 写新组件时的底线

```cpp
class CounterButtonBuilder {
    struct CounterButtonState {
        int clicks = 0;
    };

public:
    CounterButtonBuilder(eui::Ui& ui, std::string id)
        : ui_(ui), id_(std::move(id)) {}

    CounterButtonBuilder& size(float width, float height) {
        width_ = width;
        height_ = height;
        return *this;
    }

    CounterButtonBuilder& text(std::string value) {
        text_ = std::move(value);
        return *this;
    }

    CounterButtonBuilder& onClick(std::function<void(int)> callback) {
        onClick_ = std::move(callback);
        return *this;
    }

    void build() {
        CounterButtonState& state = ui_.state<CounterButtonState>(id_ + ".state");
        const float w = std::max(1.0f, width_);
        const float h = std::max(1.0f, height_);

        ui_.stack(id_)
            .size(w, h)
            .visualStateFrom(id_ + ".hit", 0.965f)
            .content([&] {
                ui_.rect(id_ + ".hit")
                    .size(w, h)
                    .states(normal_, hover_, pressed_)
                    .radius(std::min(12.0f, h * 0.5f))
                    .onClick([&state, callback = onClick_] {
                        ++state.clicks;
                        if (callback) {
                            callback(state.clicks);
                        }
                    })
                    .build();

                ui_.text(id_ + ".label")
                    .size(w, h)
                    .text(text_ + " " + std::to_string(state.clicks))
                    .fontSize(15.0f)
                    .lineHeight(19.0f)
                    .color({0.94f, 0.96f, 1.0f, 1.0f})
                    .horizontalAlign(eui::HorizontalAlign::Center)
                    .verticalAlign(eui::VerticalAlign::Center)
                    .build();
            })
            .build();
    }

private:
    eui::Ui& ui_;
    std::string id_;
    std::string text_ = "Clicks";
    std::function<void(int)> onClick_;
    float width_ = 160.0f;
    float height_ = 42.0f;
    eui::Color normal_{0.12f, 0.14f, 0.18f, 1.0f};
    eui::Color hover_{0.16f, 0.18f, 0.24f, 1.0f};
    eui::Color pressed_{0.10f, 0.12f, 0.16f, 1.0f};
};

inline CounterButtonBuilder counterButton(eui::Ui& ui, const std::string& id) {
    return CounterButtonBuilder(ui, id);
}
```

使用方式：

```cpp
counterButton(ui, "demo.counter")
    .size(180.0f, 44.0f)
    .text("Counter")
    .onClick([](int clicks) {
        // 业务层只接收事件，不负责释放组件内部 click state。
    })
    .build();
```

重点：

- 组件只是 DSL 组合。视觉、交互和动画都声明到 DSL 树上，由 Runtime 统一执行。
- 组件内部交互态使用 `ui.state<T>(id)`，不要写组件级 `static unordered_map`。
- 页面业务状态仍由页面持有，例如 `open`、`selected`、`checked`、输入文本和数据源。
- 组件 id 必须稳定，内部子节点使用 `id_ + ".name"`，避免和同级组件互相污染。
- 组件放进 Loader 后不需要自己处理生命周期：`destroyOnHide` 会释放 `ui.state`，`keepAlive` 会保留。
